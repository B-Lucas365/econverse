import { Promo } from "../CardPromo"
import { Categories } from "../Categories"

export const Main = () => {
    return(
        <main>
            <Promo />
            <Categories />
        </main>
    )
}