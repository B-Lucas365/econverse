import { Promo } from "../CardPromo"
import { Categories } from "../Categories"
import { Products } from "../Products"

export const Main = () => {
    return(
        <main>
            <Promo />
            <Categories />
            <Products />
        </main>
    )
}